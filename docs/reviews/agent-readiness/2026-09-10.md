# Agent-readiness audit and implementation review

Date: 2026-09-10. Origin: https://schngn.com.
External audit: https://isitagentready.com/schngn.com (default scan: 20/100).
Tracking: https://github.com/miktomic/mg-hq/issues/7.

## Findings and disposition

| Finding | Disposition |
| --- | --- |
| No useful discovery Link header | Public documentation and skills links added |
| Markdown negotiation absent | 136 build-derived public documents across 17 locales |
| Content Signals absent | Public search/AI input allowed; training excluded |
| Skill discovery absent | Existing local-runtime skill and references packaged with SHA-256 |
| ARD absent | Truthful public documentation catalog added |
| Robots/sitemap | Already passed; strengthened real-file/coverage checks |
| OAuth, auth.md registration, remote MCP, DNS-AID, A2A, Web Bot Auth, commerce | Unsupported capabilities; applicability and adoption triggers documented |
| WebMCP | Future browser capability; existing local MCP/CLI remains the supported runtime |

## Verification

- Full `bun run check`: 444 tests passed, zero failures; Svelte checks reported
  zero errors/warnings; engine/capability/agent/web builds and compiled agent
  smoke passed.
- Normal gate now starts an isolated local Cloudflare preview, then verifies
  all 136 public Markdown and HTML responses, media quality preferences, HEAD,
  Vary, discovery MIME/CORS, exact skill digest, and real 404 responses.
- Added runtime-asset and unauthenticated-account probes. They caught and fixed
  dispatch exclusions that initially prevented dynamic env.js and API routing.
  Local authentication configuration may return the exact configured-unavailable
  error; production requires the ordinary 401 authentication-required response.
- Browser landing and agent guide rendered; language control hydrated after the
  runtime routing fix. No new console error appeared on the subsequent reload.
- Conversion keeps footer disclaimers and legal links; regression test verifies
  footer limitations are retained. The generated content is read from public
  build artifacts; request cookies, authorization, and query strings are excluded.
- Existing unrelated moving-window work remains intact. No trip data was entered.

## Release status

Implemented and verified locally. Not committed, pushed, merged, or deployed by
this audit. The external score is the production baseline, not a measured
post-change score. Re-run production smoke and the external scanner after the
normal protected release. Keep HQ #7 open until production verification succeeds.

Ongoing rules: `docs/agent-readiness.md`; project instructions link the contract.

Specialist review fixes: normalize Accept whitespace and duplicate quality ranges;
use a five-minute browser cache for public Markdown with shared CDN variant
caching disabled. The archive includes only three explicitly curated files.

## Reusable skill follow-up

Created and locally installed the shared `agent-readiness` skill in MG HQ. Its
catalog includes the external scanner plus canonical/indexability, content
fidelity, accessibility, HTTP/cache behavior, auth/tool trust and operational
checks, with explicit conditional/manual/live-only applicability.

SCHNGN now validates every generated public document's title, language,
main/heading, canonical URL, robots metadata, JSON-LD syntax and retained
source/footer links. Expanded HTTP checks verify llms.txt, both cache request
orders, wildcard preferences, HEAD variation and synthetic input isolation.
The final production build is checked before inactive upload, and the documented
manual deploy command checks its final build too.

Verification: full `bun run check` passed (447 tests and 136 public HTTP document
checks), followed by the new deploy-command regression test. The latter uses
inert executables in a temporary directory to prove a readiness failure prevents
upload and that the last build precedes the gate. No deploy command ran against
Cloudflare. Source changes remain uncommitted/unpushed and production is unchanged.

## Release validation

The full release tree passed `bun run prepr` on 2026-09-10: 448 unit tests,
all builds and Cloudflare HTTP checks, and 40 browser tests. This includes the
moving-window calculator work and its design/research artifacts. The release
gate found and fixed a local offline-cache regression: Vite raw SVG imports
must retain their exact query string in both local-only cache allowlists.
Tests verify those imports cache locally while extra input parameters and
non-loopback source paths remain excluded. Earlier local-only notes above are
historical checkpoints; release and production evidence is tracked in the PR.
